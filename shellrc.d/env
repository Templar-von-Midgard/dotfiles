export NINJA_STATUS='[%f/%t %o/s] (%e s)'

if [ -n "$SSH_CONNECTION" ]; then
  export EDITOR=nano
else
  export EDITOR=subl
fi

# For Android sdkmanager:
export SDKMANAGER_OPTS="--add-modules java.xml.bind"
